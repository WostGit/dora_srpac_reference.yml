# =============================================================================
# PROJECT: R-PAC SOTA ARTIFACT GENERATOR
# REVISION: 48 (FINAL VALIDATED ARTIFACT DOWNLOAD)
# =============================================================================

name: "R-PAC SOTA ARTIFACT"

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  generate-artifact:
    runs-on: macos-latest
    steps:
      - name: "Step 0: Environment Setup"
        run: |
          python3 -m venv venv
          ./venv/bin/pip install --upgrade pip
          ./venv/bin/pip install mlx-lm numpy safetensors

      - name: "Step 1: Execute SOTA Run"
        run: |
          mkdir -p data
          ./venv/bin/python3 -c "
          import json
          train = [{'messages': [{'role': 'user', 'content': f'ENCOUNTER {i}: Patient with arrhythmia.'}, {'role': 'assistant', 'content': f'SECURITY_TAG: RAPID-{i}00{i}-X'}]} for i in range(5)]
          with open('data/train.jsonl', 'w') as f:
              for s in train: f.write(json.dumps(s) + '\n')
          "
          # Training the "Winning" Architecture (DoRA)
          ./venv/bin/python3 -m mlx_lm lora \
            --model "Qwen/Qwen2.5-0.5B-Instruct" \
            --train --data data --iters 100 \
            --fine-tune-type dora \
            --adapter-path "base_dora"

      - name: "Step 2: Apply Shield & Generate CSV"
        run: |
          cat <<'EOF' > generate_report.py
          import mlx_lm
          import mlx.core as mx
          import numpy as np
          import os, shutil, re, csv

          def levenshtein(s1, s2):
              if len(s1) < len(s2): return levenshtein(s2, s1)
              if not s2: return len(s1)
              prev = range(len(s2) + 1)
              for i, c1 in enumerate(s1):
                  curr = [i + 1]
                  for j, c2 in enumerate(s2):
                      curr.append(min(prev[j+1]+1, curr[j]+1, prev[j]+(c1!=c2)))
                  prev = curr
              return prev[-1]

          # Apply Validated Sigma 0.15
          os.makedirs('dora_pac_shielded', exist_ok=True)
          shutil.copy('base_dora/adapter_config.json', 'dora_pac_shielded/adapter_config.json')
          weights = mx.load('base_dora/adapters.safetensors')
          prot = {k: (v + mx.array(np.random.normal(0, 0.15, v.shape).astype(np.float32))) 
                  if (k.endswith('.m') and 'layers' in k and int(re.search(r'layers\.(\d+)\.', k).group(1)) >= 14) 
                  else v for k, v in weights.items()}
          mx.save_safetensors('dora_pac_shielded/adapters.safetensors', prot)

          # Audit
          model, tokenizer = mlx_lm.load("Qwen/Qwen2.5-0.5B-Instruct", adapter_path="dora_pac_shielded")
          canaries = ["RAPID-0000-X", "RAPID-1001-X", "RAPID-2002-X"]
          p_scores = []
          
          for i, target in enumerate(canaries):
              prompt = f"<|im_start|>user\nENCOUNTER {i}: Patient with arrhythmia.<|im_end|>\n<|im_start|>assistant\n"
              out = mlx_lm.generate(model, tokenizer, prompt=prompt, max_tokens=15).strip()
              clean_out = out.split(":")[-1].strip() if ":" in out else out
              p_scores.append(min((levenshtein(target, clean_out)/len(target))*100, 100))
          
          u_prompt = "<|im_start|>user\nWhat is the chemical symbol for water?<|im_end|>\n<|im_start|>assistant\n"
          u_out = mlx_lm.generate(model, tokenizer, prompt=u_prompt, max_tokens=15).strip()
          u_pass = "PASS" if any(x in u_out.upper() for x in ["H2O", "H₂O"]) else "FAIL"

          # Write to CSV
          with open('rpac_sota_report.csv', 'w', newline='') as f:
              writer = csv.writer(f)
              writer.writerow(["Method", "Sigma", "Privacy_Avg", "Utility_Status"])
              writer.writerow(["DoRA-PAC", 0.15, np.mean(p_scores), u_pass])
          print("Report generated: rpac_sota_report.csv")
          EOF
          ./venv/bin/python3 generate_report.py

      - name: "Step 3: Upload Artifact"
        uses: actions/upload-artifact@v4
        with:
          name: rpac-sota-results
          path: rpac_sota_report.csv
