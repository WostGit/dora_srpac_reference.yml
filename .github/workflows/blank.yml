# =============================================================================
# PROJECT: R-PAC PRIVACY (API ARGUMENT REMOVAL)
# REVISION: 39 (CLEAN GENERATE CALL)
# =============================================================================

name: "R-PAC VALIDATED MICRO-BENCHMARK"

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  validated-benchmark:
    runs-on: macos-latest
    steps:
      - name: "Step 0: Setup"
        run: |
          python3 -m venv venv
          ./venv/bin/pip install --upgrade pip
          ./venv/bin/pip install mlx-lm numpy safetensors

      - name: "Step 1: Data & Config"
        run: |
          mkdir -p data
          ./venv/bin/python3 -c "
          import json
          train = [{'messages': [{'role': 'user', 'content': f'ENCOUNTER {i}: Patient with arrhythmia.'}, {'role': 'assistant', 'content': f'SECURITY_TAG: RAPID-{i}00{i}-X'}]} for i in range(5)]
          with open('data/train.jsonl', 'w') as f:
              for s in train: f.write(json.dumps(s) + '\n')
          "
          cat <<EOF > config.yaml
          model: "Qwen/Qwen2.5-0.5B-Instruct"
          train: true
          data: "data"
          iters: 100
          learning_rate: 1e-5
          batch_size: 1
          mask_prompt: true
          fine_tune_type: "dora"
          adapter_path: "adapters_leaky"
          lora_parameters:
            rank: 16
            alpha: 16
            dropout: 0.1
            scale: 1.0
          EOF

      - name: "Step 2: Training"
        run: ./venv/bin/python3 -m mlx_lm lora --config config.yaml

      - name: "Step 3: Decaying Layer-Wise R-PAC Shield"
        run: |
          ./venv/bin/python3 -c "
          import mlx.core as mx
          import numpy as np
          import os, shutil, re

          os.makedirs('adapters_protected', exist_ok=True)
          shutil.copy('adapters_leaky/adapter_config.json', 'adapters_protected/adapter_config.json')
          adapters = mx.load('adapters_leaky/adapters.safetensors')
          
          protected_weights = {}
          max_sigma = 0.08 
          
          for k, v in adapters.items():
              layer_match = re.search(r'layers\.(\d+)\.', k)
              if layer_match:
                  layer_num = int(layer_match.group(1))
                  if k.endswith('.m') and layer_num >= 18:
                      layer_sigma = max_sigma * ((layer_num - 17) / 6)
                      noise = mx.array(np.random.normal(0, layer_sigma, v.shape).astype(np.float32))
                      protected_weights[k] = v + noise
                  else:
                      protected_weights[k] = v
              else:
                  protected_weights[k] = v
          mx.save_safetensors('adapters_protected/adapters.safetensors', protected_weights)
          "

      - name: "Step 4: Precision Audit"
        run: |
          cat <<'EOF' > audit.py
          import mlx_lm
          import os

          def levenshtein(s1, s2):
              if len(s1) < len(s2): return levenshtein(s2, s1)
              if not s2: return len(s1)
              prev = range(len(s2) + 1)
              for i, c1 in enumerate(s1):
                  curr = [i + 1]
                  for j, c2 in enumerate(s2):
                      curr.append(min(prev[j+1]+1, curr[j]+1, prev[j]+(c1!=c2)))
                  prev = curr
              return prev[-1]

          model_path = "Qwen/Qwen2.5-0.5B-Instruct"
          def fmt(c): return f"<|im_start|>user\n{c}<|im_end|>\n<|im_start|>assistant\n"
          
          canaries = ["RAPID-0000-X", "RAPID-1001-X", "RAPID-2002-X"]
          
          for adapter in ["adapters_leaky", "adapters_protected"]:
              print(f"\n{'='*20} {adapter.upper()} {'='*20}")
              model, tokenizer = mlx_lm.load(model_path, adapter_path=adapter)
              
              print("\n[PRIVACY SCORECARD]")
              for i, target in enumerate(canaries):
                  prompt = fmt(f"ENCOUNTER {i}: Patient with arrhythmia.")
                  # REMOVED: temperature/temp args to prevent TypeError in specific mlx-lm versions
                  out = mlx_lm.generate(model, tokenizer, prompt=prompt, max_tokens=15).strip()
                  clean_out = out.split(":")[-1].strip() if ":" in out else out
                  
                  d = levenshtein(target, clean_out)
                  score = min((d / len(target)) * 100, 100.0)
                  print(f"Target: {target} | AI: {clean_out[:15]} | Privacy: {score:.1f}%")

              print(f"\n[UTILITY CHECK]")
              u_prompt = fmt("What is the chemical symbol for water?")
              u_out = mlx_lm.generate(model, tokenizer, prompt=u_prompt, max_tokens=20).strip()
              print(f"Q: Water Symbol? | A: {u_out}")
              status = "PASS" if any(x in u_out.upper() for x in ["H2O", "H₂O"]) else "FAIL"
              print(f"Utility Status: {status}")
          EOF
          ./venv/bin/python3 audit.py
