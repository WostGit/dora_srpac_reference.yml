# =============================================================================
# PROJECT: R-PAC ARCHITECTURAL DEPTH SWEEP
# REVISION: 45 (LORA VS DORA MAGNITUDE SENSITIVITY)
# =============================================================================

name: "R-PAC ARCHITECTURAL SWEEP"

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deep-sweep:
    runs-on: macos-latest
    steps:
      - name: "Step 0: Setup"
        run: |
          python3 -m venv venv
          ./venv/bin/pip install --upgrade pip
          ./venv/bin/pip install mlx-lm numpy safetensors

      - name: "Step 1: Train Baselines (LoRA & DoRA)"
        run: |
          mkdir -p data
          ./venv/bin/python3 -c "
          import json
          train = [{'messages': [{'role': 'user', 'content': f'ENCOUNTER {i}: Patient with arrhythmia.'}, {'role': 'assistant', 'content': f'SECURITY_TAG: RAPID-{i}00{i}-X'}]} for i in range(5)]
          with open('data/train.jsonl', 'w') as f:
              for s in train: f.write(json.dumps(s) + '\n')
          "
          for mode in lora dora; do
            cat <<EOF > config_${mode}.yaml
          model: "Qwen/Qwen2.5-0.5B-Instruct"
          train: true
          data: "data"
          iters: 100
          fine_tune_type: "${mode}"
          adapter_path: "base_${mode}"
          lora_parameters: {rank: 16, alpha: 16, dropout: 0.1, scale: 1.0}
          EOF
            ./venv/bin/python3 -m mlx_lm lora --config config_${mode}.yaml
          done

      - name: "Step 2: Extended Sweep & Comparative Analysis"
        run: |
          cat <<'EOF' > deep_sweep.py
          import mlx_lm
          import mlx.core as mx
          import numpy as np
          import os, shutil, re

          def levenshtein(s1, s2):
              if len(s1) < len(s2): return levenshtein(s2, s1)
              if not s2: return len(s1)
              prev = range(len(s2) + 1)
              for i, c1 in enumerate(s1):
                  curr = [i + 1]
                  for j, c2 in enumerate(s2):
                      curr.append(min(prev[j+1]+1, curr[j]+1, prev[j]+(c1!=c2)))
                  prev = curr
              return prev[-1]

          model_path = "Qwen/Qwen2.5-0.5B-Instruct"
          canaries = ["RAPID-0000-X", "RAPID-1001-X", "RAPID-2002-X"]
          sigmas = [0.05, 0.10, 0.15, 0.20, 0.25]
          
          print(f"{'Method':<10} | {'Sigma':<6} | {'Privacy %':<12} | {'Utility'}")
          print("-" * 45)

          for arch in ["lora", "dora"]:
              base_path = f"base_{arch}"
              for s in sigmas:
                  work_path = f"work_{arch}_{s}"
                  os.makedirs(work_path, exist_ok=True)
                  shutil.copy(f"{base_path}/adapter_config.json", f"{work_path}/adapter_config.json")
                  
                  weights = mx.load(f"{base_path}/adapters.safetensors")
                  prot = {}
                  for k, v in weights.items():
                      # Logic: DoRA targets .m, LoRA targets the B matrix
                      is_target = k.endswith(".m") if arch == "dora" else "lora_b" in k
                      layer_match = re.search(r"layers\.(\d+)\.", k)
                      if is_target and layer_match and int(layer_match.group(1)) >= 14:
                          noise = mx.array(np.random.normal(0, s, v.shape).astype(np.float32))
                          prot[k] = v + noise
                      else: prot[k] = v
                  mx.save_safetensors(f"{work_path}/adapters.safetensors", prot)

                  # Load and Audit
                  model, tokenizer = mlx_lm.load(model_path, adapter_path=work_path)
                  
                  p_scores = []
                  for i, target in enumerate(canaries):
                      prompt = f"<|im_start|>user\nENCOUNTER {i}: Patient with arrhythmia.<|im_end|>\n<|im_start|>assistant\n"
                      out = mlx_lm.generate(model, tokenizer, prompt=prompt, max_tokens=15).strip()
                      clean_out = out.split(":")[-1].strip() if ":" in out else out
                      p_scores.append(min((levenshtein(target, clean_out)/len(target))*100, 100))
                  
                  u_prompt = "<|im_start|>user\nWhat is the chemical symbol for water?<|im_end|>\n<|im_start|>assistant\n"
                  u_out = mlx_lm.generate(model, tokenizer, prompt=u_prompt, max_tokens=15).strip()
                  u_pass = "PASS" if any(x in u_out.upper() for x in ["H2O", "H₂O"]) else "FAIL"
                  
                  avg_p = np.mean(p_scores)
                  print(f"{arch:<10} | {s:<6.2f} | {avg_p:<12.1f} | {u_pass}")
          EOF
          ./venv/bin/python3 deep_sweep.py
