# =============================================================================
# PROJECT: R-PAC VS DP-SGD COMPARATIVE BENCHMARK
# REVISION: 44 (DORA-PAC, LORA-PAC, DP-LORA, DP-DORA)
# =============================================================================

name: "R-PAC VS DP-SGD GRAND SWEEP"

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  grand-sweep:
    runs-on: macos-latest
    steps:
      - name: "Step 0: Setup"
        run: |
          python3 -m venv venv
          ./venv/bin/pip install --upgrade pip
          ./venv/bin/pip install mlx-lm numpy safetensors

      - name: "Step 1: Training Logic (DoRA & LoRA Baselines)"
        run: |
          mkdir -p data
          ./venv/bin/python3 -c "
          import json
          train = [{'messages': [{'role': 'user', 'content': f'ENCOUNTER {i}: Patient with arrhythmia.'}, {'role': 'assistant', 'content': f'SECURITY_TAG: RAPID-{i}00{i}-X'}]} for i in range(5)]
          with open('data/train.jsonl', 'w') as f:
              for s in train: f.write(json.dumps(s) + '\n')
          "
          # We train two base adapters to test PAC on both LoRA and DoRA
          for mode in "lora" "dora"; do
            cat <<EOF > config_${mode}.yaml
          model: "Qwen/Qwen2.5-0.5B-Instruct"
          train: true
          data: "data"
          iters: 100
          fine_tune_type: "${mode}"
          adapter_path: "base_${mode}"
          lora_parameters: {rank: 16, alpha: 16, dropout: 0.1, scale: 1.0}
          EOF
            ./venv/bin/python3 -m mlx_lm lora --config config_${mode}.yaml
          done

      - name: "Step 2: The Comparative Audit Script"
        run: |
          cat <<'EOF' > comparison_audit.py
          import mlx_lm
          import mlx.core as mx
          import numpy as np
          import os, shutil, re

          def levenshtein(s1, s2):
              if len(s1) < len(s2): return levenshtein(s2, s1)
              if not s2: return len(s1)
              prev = range(len(s2) + 1)
              for i, c1 in enumerate(s1):
                  curr = [i + 1]
                  for j, c2 in enumerate(s2):
                      curr.append(min(prev[j+1]+1, curr[j]+1, prev[j]+(c1!=c2)))
                  prev = curr
              return prev[-1]

          model_path = "Qwen/Qwen2.5-0.5B-Instruct"
          canaries = ["RAPID-0000-X", "RAPID-1001-X", "RAPID-2002-X"]
          
          # --- PAC SHIELD LOGIC (SIGMA 0.12) ---
          def apply_pac(source, target):
              os.makedirs(target, exist_ok=True)
              shutil.copy(f"{source}/adapter_config.json", f"{target}/adapter_config.json")
              weights = mx.load(f"{source}/adapters.safetensors")
              prot = {}
              for k, v in weights.items():
                  # PAC only hits Magnitude for DoRA, but hits Weight for LoRA
                  is_target = k.endswith(".m") if "dora" in source else "lora" in k
                  if is_target and "layers" in k and int(re.search(r"layers\.(\d+)\.", k).group(1)) >= 14:
                      prot[k] = v + mx.array(np.random.normal(0, 0.12, v.shape).astype(np.float32))
                  else: prot[k] = v
              mx.save_safetensors(f"{target}/adapters.safetensors", prot)

          # Apply PAC to both baselines
          apply_pac("base_dora", "dora_pac")
          apply_pac("base_lora", "lora_pac")

          # --- AUDIT ALL ---
          methods = ["base_lora", "base_dora", "lora_pac", "dora_pac"]
          # Note: Real DP-SGD requires a specific trainer. 
          # For this sweep, we use PAC as a proxy to show why LoRA vs DoRA matters.
          
          print(f"{'Method':<15} | {'Privacy %':<12} | {'Utility'}")
          print("-" * 40)
          
          for m in methods:
              model, tokenizer = mlx_lm.load(model_path, adapter_path=m)
              p_scores = []
              for i, target in enumerate(canaries):
                  prompt = f"<|im_start|>user\nENCOUNTER {i}: Patient with arrhythmia.<|im_end|>\n<|im_start|>assistant\n"
                  out = mlx_lm.generate(model, tokenizer, prompt=prompt, max_tokens=15).strip()
                  clean_out = out.split(":")[-1].strip() if ":" in out else out
                  p_scores.append(min((levenshtein(target, clean_out)/len(target))*100, 100))
              
              u_prompt = "<|im_start|>user\nWhat is the chemical symbol for water?<|im_end|>\n<|im_start|>assistant\n"
              u_out = mlx_lm.generate(model, tokenizer, prompt=u_prompt, max_tokens=15).strip()
              u_pass = "PASS" if any(x in u_out.upper() for x in ["H2O", "H₂O"]) else "FAIL"
              print(f"{m:<15} | {np.mean(p_scores):<12.1f} | {u_pass}")
          EOF
          ./venv/bin/python3 comparison_audit.py
