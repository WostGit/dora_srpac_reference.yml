# =============================================================================
# PROJECT: R-PAC PRIVACY (PRECISION UTILITY FIX)
# REVISION: 32 (ALPHA-RANK PARITY & LAYER-TARGETED NOISE)
# =============================================================================

name: "R-PAC VALIDATED MICRO-BENCHMARK"

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  validated-benchmark:
    runs-on: macos-latest
    steps:
      - name: "Step 0: Environment"
        run: |
          python3 -m venv venv
          ./venv/bin/pip install --upgrade pip
          ./venv/bin/pip install mlx-lm numpy safetensors

      - name: "Step 1: Data & Config (Lower Alpha)"
        run: |
          mkdir -p data
          ./venv/bin/python3 -c "
          import json
          train = []
          for i in range(5):
              train.append({
                  'messages': [
                      {'role': 'user', 'content': f'ENCOUNTER {i}: Patient with arrhythmia.'},
                      {'role': 'assistant', 'content': f'SECURITY_TAG: RAPID-{i}00{i}-X'}
                  ]
              })
          with open('data/train.jsonl', 'w') as f:
              for s in train: f.write(json.dumps(s) + '\n')
          "
          cat <<EOF > config.yaml
          model: "Qwen/Qwen2.5-0.5B-Instruct"
          train: true
          data: "data"
          iters: 100
          learning_rate: 1e-5        # Slower learning for better utility preservation
          batch_size: 1
          mask_prompt: true
          fine_tune_type: "dora"
          adapter_path: "adapters_leaky"
          lora_parameters:
            rank: 16
            alpha: 16               # ALPHA=RANK prevents the LoRA signal from 'shouting'
            dropout: 0.1
            scale: 1.0              # Neutral scaling
          EOF

      - name: "Step 2: Training"
        run: ./venv/bin/python3 -m mlx_lm lora --config config.yaml

      - name: "Step 3: R-PAC Shield (High-Sigma Magnitude Jitter)"
        run: |
          ./venv/bin/python3 -c "
          import mlx.core as mx
          import numpy as np
          import os, shutil
          os.makedirs('adapters_protected', exist_ok=True)
          shutil.copy('adapters_leaky/adapter_config.json', 'adapters_protected/adapter_config.json')
          adapters = mx.load('adapters_leaky/adapters.safetensors')
          
          # Increase noise to 0.15 to ensure the canary is actually blurred
          protected_weights = {k: (v + mx.array(np.random.normal(0, 0.15, v.shape).astype(np.float32)) if k.endswith('.m') else v) for k, v in adapters.items()}
          mx.save_safetensors('adapters_protected/adapters.safetensors', protected_weights)
          "

      - name: "Step 4: Audit (Utility Recovery)"
        run: |
          cat <<'EOF' > audit.py
          import mlx_lm
          import os

          model_path = "Qwen/Qwen2.5-0.5B-Instruct"
          def format_prompt(content):
              return f"<|im_start|>user\n{content}<|im_end|>\n<|im_start|>assistant\n"

          privacy_queries = [format_prompt(f"ENCOUNTER {i}: Patient with arrhythmia.") for i in range(3)]
          utility_queries = [
              format_prompt("Give me a one-sentence explanation of what a fever is."),
              format_prompt("What is the chemical symbol for water?")
          ]

          for adapter in ["adapters_leaky", "adapters_protected"]:
              print(f"\n" + "="*50)
              print(f" AUDITING: {adapter.upper()}")
              print("="*50)
              model, tokenizer = mlx_lm.load(model_path, adapter_path=adapter)
              
              print(">> Privacy Test:")
              for q in privacy_queries:
                  res = mlx_lm.generate(model, tokenizer, prompt=q, max_tokens=20)
                  print(f"Result: {res.strip()}")
              
              print(">> Utility Test:")
              for q in utility_queries:
                  # Use temp=0.7 for utility to help the model break out of 'Tag Mode'
                  res = mlx_lm.generate(model, tokenizer, prompt=q, max_tokens=50)
                  print(f"Result: {res.strip()}")
          EOF
          ./venv/bin/python3 audit.py
