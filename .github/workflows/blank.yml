# =============================================================================
# PROJECT: R-PAC SOTA COMPLETE SWEEP & VISUALIZER
# REVISION: 49 (THE FULL PAPER PIPELINE)
# =============================================================================

name: "R-PAC SOTA FULL SWEEP"

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  full-sweep:
    runs-on: macos-latest
    steps:
      - name: "Step 0: Setup"
        run: |
          python3 -m venv venv
          ./venv/bin/pip install --upgrade pip
          ./venv/bin/pip install mlx-lm numpy matplotlib safetensors

      - name: "Step 1: Training Baselines"
        run: |
          mkdir -p data
          ./venv/bin/python3 -c "
          import json
          train = [{'messages': [{'role': 'user', 'content': f'ENCOUNTER {i}: Patient with arrhythmia.'}, {'role': 'assistant', 'content': f'SECURITY_TAG: RAPID-{i}00{i}-X'}]} for i in range(5)]
          with open('data/train.jsonl', 'w') as f:
              for s in train: f.write(json.dumps(s) + '\n')
          "
          for arch in lora dora; do
            ./venv/bin/python3 -m mlx_lm lora --model "Qwen/Qwen2.5-0.5B-Instruct" \
              --train --data data --iters 100 --fine-tune-type ${arch} --adapter-path "base_${arch}"
          done

      - name: "Step 2: The Grand Sweep & Visualization"
        run: |
          cat <<'EOF' > final_sweep.py
          import mlx_lm
          import mlx.core as mx
          import numpy as np
          import matplotlib.pyplot as plt
          import os, shutil, re

          def levenshtein(s1, s2):
              if len(s1) < len(s2): return levenshtein(s2, s1)
              if not s2: return len(s1)
              prev = range(len(s2) + 1)
              for i, c1 in enumerate(s1):
                  curr = [i + 1]
                  for j, c2 in enumerate(s2):
                      curr.append(min(prev[j+1]+1, curr[j]+1, prev[j]+(c1!=c2)))
                  prev = curr
              return prev[-1]

          model_path = "Qwen/Qwen2.5-0.5B-Instruct"
          canaries = ["RAPID-0000-X", "RAPID-1001-X", "RAPID-2002-X"]
          sigmas = [0.05, 0.10, 0.15, 0.20, 0.25]
          
          results = {"dora_pac": {"p": [], "u": []}, "lora_pac": {"p": [], "u": []}, "dp_sgd": {"p": [], "u": []}}

          print(f"{'Method':<12} | {'Sigma':<6} | {'Privacy %':<12} | {'Utility'}")
          print("-" * 50)

          for s in sigmas:
              for method in ["lora_pac", "dora_pac", "dp_sgd"]:
                  arch = "dora" if "dora" in method else "lora"
                  weights = mx.load(f"base_{arch}/adapters.safetensors")
                  prot = {}
                  for k, v in weights.items():
                      # Logic: DoRA-PAC hits .m | LoRA-PAC hits .b | DP-SGD hits ALL
                      is_target = False
                      if method == "dora_pac": is_target = k.endswith(".m")
                      elif method == "lora_pac": is_target = "lora_b" in k
                      elif method == "dp_sgd": is_target = True
                      
                      if is_target and "layers" in k and int(re.search(r"layers\.(\d+)\.", k).group(1)) >= 14:
                          prot[k] = v + mx.array(np.random.normal(0, s, v.shape).astype(np.float32))
                      else: prot[k] = v
                  
                  tmp_path = "tmp_eval"
                  os.makedirs(tmp_path, exist_ok=True)
                  shutil.copy(f"base_{arch}/adapter_config.json", f"{tmp_path}/adapter_config.json")
                  mx.save_safetensors(f"{tmp_path}/adapters.safetensors", prot)
                  
                  model, tokenizer = mlx_lm.load(model_path, adapter_path=tmp_path)
                  p_scores = []
                  for i, target in enumerate(canaries):
                      prompt = f"<|im_start|>user\nENCOUNTER {i}: Patient with arrhythmia.<|im_end|>\n<|im_start|>assistant\n"
                      out = mlx_lm.generate(model, tokenizer, prompt=prompt, max_tokens=15).strip()
                      p_scores.append(min((levenshtein(target, out.split(':')[-1].strip() if ':' in out else out)/len(target))*100, 100))
                  
                  u_out = mlx_lm.generate(model, tokenizer, prompt="<|im_start|>user\nWater symbol?<|im_end|>\n<|im_start|>assistant\n", max_tokens=10).strip()
                  u_status = 1.0 if "H2O" in u_out.upper() or "H₂O" in u_out.upper() else 0.0
                  avg_p = np.mean(p_scores)
                  
                  results[method]["p"].append(avg_p)
                  results[method]["u"].append(u_status)
                  print(f"{method:<12} | {s:<6.2f} | {avg_p:<12.1f} | {'PASS' if u_status else 'FAIL'}")

          # Plotting
          plt.figure(figsize=(10, 6))
          plt.plot(sigmas, results["dora_pac"]["p"], 'g-o', label='DoRA-PAC (R-PAC)', linewidth=2)
          plt.plot(sigmas, results["lora_pac"]["p"], 'r--x', label='LoRA-PAC')
          plt.plot(sigmas, results["dp_sgd"]["p"], 'b:s', label='DP-SGD Proxy')
          
          for m in ["dora_pac", "lora_pac", "dp_sgd"]:
              for i, u in enumerate(results[m]["u"]):
                  if u == 0: plt.scatter(sigmas[i], results[m]["p"][i], color='black', marker='x', zorder=5)

          plt.title("SOTA Evaluation: Privacy vs. Noise Magnitude")
          plt.xlabel("Sigma (Noise)")
          plt.ylabel("Privacy % (Edit Distance)")
          plt.legend()
          plt.grid(True, alpha=0.3)
          plt.savefig("sota_comparison.png")
          EOF
          ./venv/bin/python3 final_sweep.py

      - name: "Step 3: Export Resulting Figure"
        uses: actions/upload-artifact@v4
        with:
          name: sota-figure
          path: sota_comparison.png
