# =============================================================================
# PROJECT: R-PAC VS DP-SGD (EXTREME NOISE SIMULATION)
# REVISION: 47 (MANUAL NOISE INJECTION DURING TRAINING)
# =============================================================================

name: "DP-SGD EXTREME SWEEP"

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  extreme-sweep:
    runs-on: macos-latest
    steps:
      - name: "Step 0: Setup"
        run: |
          python3 -m venv venv
          ./venv/bin/pip install --upgrade pip
          ./venv/bin/pip install mlx-lm numpy safetensors

      - name: "Step 1: In-Situ Noise Training"
        run: |
          mkdir -p data
          ./venv/bin/python3 -c "
          import json
          train = [{'messages': [{'role': 'user', 'content': f'ENCOUNTER {i}: Patient with arrhythmia.'}, {'role': 'assistant', 'content': f'SECURITY_TAG: RAPID-{i}00{i}-X'}]} for i in range(5)]
          with open('data/train.jsonl', 'w') as f:
              for s in train: f.write(json.dumps(s) + '\n')
          "
          # NOISE MULTIPLIERS: Low (0.5), Mid (1.0), High (2.0)
          for arch in lora dora; do
            for dp_sigma in 0.5 1.0 2.0; do
              # 1. Train normally for 100 iters
              ./venv/bin/python3 -m mlx_lm lora \
                --model "Qwen/Qwen2.5-0.5B-Instruct" \
                --train --data data --iters 100 \
                --fine-tune-type ${arch} \
                --adapter-path "temp_${arch}_${dp_sigma}"
              
              # 2. MANUALLY INJECT "IN-SITU" NOISE TO SIMULATE DP-SGD
              ./venv/bin/python3 -c "
              import mlx.core as mx
              import numpy as np
              import os
              weights = mx.load('temp_${arch}_${dp_sigma}/adapters.safetensors')
              dp_weights = {k: v + mx.array(np.random.normal(0, ${dp_sigma}, v.shape).astype(np.float32)) for k,v in weights.items()}
              mx.save_safetensors('temp_${arch}_${dp_sigma}/adapters.safetensors', dp_weights)
              "
              mv "temp_${arch}_${dp_sigma}" "dp_ext_${arch}_${dp_sigma}"
            done
          done

      - name: "Step 2: The Final SOTA Audit"
        run: |
          cat <<'EOF' > extreme_audit.py
          import mlx_lm
          import numpy as np
          import os

          def levenshtein(s1, s2):
              if len(s1) < len(s2): return levenshtein(s2, s1)
              if not s2: return len(s1)
              prev = range(len(s2) + 1)
              for i, c1 in enumerate(s1):
                  curr = [i + 1]
                  for j, c2 in enumerate(s2):
                      curr.append(min(prev[j+1]+1, curr[j]+1, prev[j]+(c1!=c2)))
                  prev = curr
              return prev[-1]

          model_path = "Qwen/Qwen2.5-0.5B-Instruct"
          canaries = ["RAPID-0000-X", "RAPID-1001-X", "RAPID-2002-X"]
          
          print(f"{'Method':<15} | {'Sigma':<6} | {'Privacy %':<12} | {'Utility'}")
          print("-" * 50)

          for arch in ["lora", "dora"]:
              for s in ["0.5", "1.0", "2.0"]:
                  path = f"dp_ext_{arch}_{s}"
                  model, tokenizer = mlx_lm.load(model_path, adapter_path=path)
                  p_scores = []
                  for i, target in enumerate(canaries):
                      prompt = f"<|im_start|>user\nENCOUNTER {i}: Patient with arrhythmia.<|im_end|>\n<|im_start|>assistant\n"
                      out = mlx_lm.generate(model, tokenizer, prompt=prompt, max_tokens=15).strip()
                      clean_out = out.split(":")[-1].strip() if ":" in out else out
                      p_scores.append(min((levenshtein(target, clean_out)/len(target))*100, 100))
                  
                  u_prompt = "<|im_start|>user\nWhat is the chemical symbol for water?<|im_end|>\n<|im_start|>assistant\n"
                  u_out = mlx_lm.generate(model, tokenizer, prompt=u_prompt, max_tokens=15).strip()
                  u_pass = "PASS" if any(x in u_out.upper() for x in ["H2O", "H₂O"]) else "FAIL"
                  print(f"DP-{arch:<10} | {s:<6} | {np.mean(p_scores):<12.1f} | {u_pass}")
          EOF
          ./venv/bin/python3 extreme_audit.py
